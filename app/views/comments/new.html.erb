<h1>New Comment</h1>

<%= form_with(model: [@post, @comment], url: post_comments_path(@post), method: :post) do |form| %>
  <%= form.label :content %>
  <%= form.text_area :content %>

  <%= form.submit 'Create Comment' %>
<% end %>
